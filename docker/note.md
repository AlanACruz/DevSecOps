# Docker

## Install Docker
'''
sudo apt install docker.io
'''

## Build/Save
```
docker build --tag ... -file ...
docker save --output "....tar" myimage:latest
```

## Tag & Push Commands
```
docker tag
docker push
```
